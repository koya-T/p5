function setup() {
    createCanvas(windowWidth, windowHeight);
}

function draw() {
    for(let i = 0; i < 22; i++){
        for(let j = 0; j < 12; j++){
            // translate();
            square(10, 10, 10);
        }
    }
}